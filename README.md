# -_aplicacion_MVC_empresarial_-- :.
Aplicacion MVC Empresarial:

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/f2ad65c6-4f51-4be9-bfb1-05cf02467441" />    

<img width="2554" height="1079" alt="image" src="https://github.com/user-attachments/assets/d6e2a7a2-6759-4e4b-8181-4c54e9fe4519" />    

```
Visual Studio 2022 + ASP.NET Core MVC + C# + JavaScript + HTML + CSS + Oracle 19c.
Desarrollaremos una aplicación web tipo MVC empresarial con:
ASP.NET Core MVC (.NET 8)
Arquitectura por capas
Oracle Database 19c
CRUD completo
Frontend con HTML + CSS + JavaScript
Entity Framework Core + Oracle
Formularios modernos
Procesamiento y persistencia de información en Oracle 19c
Compatible con Visual Studio 2022

Arquitectura del Proyecto
SistemaMVCOracle/
│
├── Controllers/
├── Models/
├── Views/
├── wwwroot/
│   ├── css/
│   ├── js/
│
├── Data/
├── Services/
├── appsettings.json
├── Program.cs
└── SistemaMVCOracle.csproj

Tecnologias Utilizadas:
Tecnología	      Descripción
ASP.NET Core MVC	Framework Web
C#	              Backend
JavaScript	      Frontend dinámico
HTML5	            Estructura
CSS3	            Estilos
Oracle 19c	      Base de datos
Entity Framework  Core	ORM
Visual Studio2022	IDE.

PASO 1 — Crear Proyecto:
En Visual Studio 2022
Archivo → Nuevo Proyecto
Seleccionar:
ASP.NET Core Web App (Model-View-Controller)
Framework
.NET 8
Nombre
SistemaMVCOracle.

PASO 2 — Instalar Paquetes NuGet:
Administrador de Paquetes NuGet
Instalar:
Microsoft.EntityFrameworkCore
Microsoft.EntityFrameworkCore.Tools
Oracle.EntityFrameworkCore
Oracle.ManagedDataAccess.Core.

PASO 3 — Crear Base de Datos Oracle 19c:
Script Oracle
CREATE TABLE PRODUCTOS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NOMBRE VARCHAR2(100),
    PRECIO NUMBER(10,2),
    STOCK NUMBER,
    FECHA_REGISTRO DATE DEFAULT SYSDATE,
    CONSTRAINT PK_PRODUCTOS PRIMARY KEY(ID)
);

PASO 4 — Configurar appsettings.json:
{
  "ConnectionStrings": {
    "OracleConnection": "User Id=SYSTEM;Password=oracle;Data Source=localhost:1521/XEPDB1;"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}

PASO 5 — Modelo Producto:
Models/Producto.cs
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;

namespace SistemaMVCOracle.Models
{
    [Table("PRODUCTOS")]
    public class Producto
    {
        [Key]
        public int ID { get; set; }

        [Required]
        public string NOMBRE { get; set; }

        public decimal PRECIO { get; set; }

        public int STOCK { get; set; }

        public DateTime FECHA_REGISTRO { get; set; }
    }
}

PASO 6 — DbContext Oracle:
Data/AppDbContext.cs
using Microsoft.EntityFrameworkCore;
using SistemaMVCOracle.Models;

namespace SistemaMVCOracle.Data
{
    public class AppDbContext : DbContext
    {
        public AppDbContext(DbContextOptions<AppDbContext> options)
            : base(options)
        {
        }

        public DbSet<Producto> Productos { get; set; }
    }
}

PASO 7 — Configurar Program.cs:
using Microsoft.EntityFrameworkCore;
using SistemaMVCOracle.Data;

var builder = WebApplication.CreateBuilder(args);

builder.Services.AddControllersWithViews();

builder.Services.AddDbContext<AppDbContext>(options =>
    options.UseOracle(
        builder.Configuration.GetConnectionString("OracleConnection")));

var app = builder.Build();

if (!app.Environment.IsDevelopment())
{
    app.UseExceptionHandler("/Home/Error");
}

app.UseStaticFiles();

app.UseRouting();

app.UseAuthorization();

app.MapControllerRoute(
    name: "default",
    pattern: "{controller=Productos}/{action=Index}/{id?}");

app.Run();

PASO 8 — Crear Controlador MVC:
Controllers/ProductosController.cs
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;
using SistemaMVCOracle.Data;
using SistemaMVCOracle.Models;

namespace SistemaMVCOracle.Controllers
{
    public class ProductosController : Controller
    {
        private readonly AppDbContext _context;

        public ProductosController(AppDbContext context)
        {
            _context = context;
        }

        public async Task<IActionResult> Index()
        {
            return View(await _context.Productos.ToListAsync());
        }

        public IActionResult Create()
        {
            return View();
        }

        [HttpPost]
        public async Task<IActionResult> Create(Producto producto)
        {
            if (ModelState.IsValid)
            {
                producto.FECHA_REGISTRO = DateTime.Now;

                _context.Productos.Add(producto);

                await _context.SaveChangesAsync();

                return RedirectToAction(nameof(Index));
            }

            return View(producto);
        }

        public async Task<IActionResult> Edit(int id)
        {
            var producto = await _context.Productos.FindAsync(id);

            return View(producto);
        }

        [HttpPost]
        public async Task<IActionResult> Edit(Producto producto)
        {
            _context.Productos.Update(producto);

            await _context.SaveChangesAsync();

            return RedirectToAction(nameof(Index));
        }

        public async Task<IActionResult> Delete(int id)
        {
            var producto = await _context.Productos.FindAsync(id);

            _context.Productos.Remove(producto);

            await _context.SaveChangesAsync();

            return RedirectToAction(nameof(Index));
        }
    }
}

PASO 9 — Vista Principal:
Views/Productos/Index.cshtml
@model IEnumerable<SistemaMVCOracle.Models.Producto>

@{
    ViewData["Title"] = "Productos";
}

<link rel="stylesheet" href="~/css/site.css" />

<h1>Lista de Productos</h1>

<a href="/Productos/Create" class="btn">
    Nuevo Producto
</a>

<table class="tabla">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Stock</th>
            <th>Fecha</th>
            <th>Acciones</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ID</td>
                <td>@item.NOMBRE</td>
                <td>@item.PRECIO</td>
                <td>@item.STOCK</td>
                <td>@item.FECHA_REGISTRO</td>

                <td>
                    <a href="/Productos/Edit/@item.ID">
                        Editar
                    </a>

                    <a href="/Productos/Delete/@item.ID">
                        Eliminar
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

PASO 10 — Vista Crear:
Views/Productos/Create.cshtml
@model SistemaMVCOracle.Models.Producto

<link rel="stylesheet" href="~/css/site.css" />

<h1>Nuevo Producto</h1>

<form method="post">

    <label>Nombre</label>
    <input asp-for="NOMBRE" />

    <label>Precio</label>
    <input asp-for="PRECIO" />

    <label>Stock</label>
    <input asp-for="STOCK" />

    <button type="submit">
        Guardar
    </button>

</form>

PASO 11 — Vista Editar:
Views/Productos/Edit.cshtml
@model SistemaMVCOracle.Models.Producto

<link rel="stylesheet" href="~/css/site.css" />

<h1>Editar Producto</h1>

<form method="post">

    <input type="hidden" asp-for="ID" />

    <label>Nombre</label>
    <input asp-for="NOMBRE" />

    <label>Precio</label>
    <input asp-for="PRECIO" />

    <label>Stock</label>
    <input asp-for="STOCK" />

    <button type="submit">
        Actualizar
    </button>

</form>

PASO 12 — CSS:
wwwroot/css/site.css
body {
    font-family: Arial;
    margin: 40px;
    background: #f4f4f4;
}

h1 {
    color: #333;
}

.tabla {
    width: 100%;
    border-collapse: collapse;
    background: white;
}

.tabla th,
.tabla td {
    border: 1px solid #ddd;
    padding: 10px;
}

.tabla th {
    background: #222;
    color: white;
}

.btn {
    padding: 10px 20px;
    background: #0d6efd;
    color: white;
    text-decoration: none;
}

form {
    background: white;
    padding: 20px;
    width: 400px;
}

input {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
}

PASO 13 — JavaScript:
wwwroot/js/site.js
document.addEventListener("DOMContentLoaded", () => {

    console.log("Sistema MVC Oracle iniciado");

});

PASO 14 — Migraciones EF Core:
Consola NuGet
Add-Migration InitialCreate
Update-Database

PASO 15 — Ejecutar Proyecto:
CTRL + F5
Resultado Final

La aplicación permitira:
✅ Registrar productos
✅ Editar productos
✅ Eliminar productos
✅ Consultar productos
✅ Persistencia en Oracle 19c
✅ Arquitectura MVC
✅ Frontend HTML/CSS/JS
✅ Entity Framework Core
✅ Visual Studio 2022

Estructura Final Esperada
SistemaMVCOracle
│
├── Controllers
│   └── ProductosController.cs
│
├── Data
│   └── AppDbContext.cs
│
├── Models
│   └── Producto.cs
│
├── Views
│   └── Productos
│       ├── Index.cshtml
│       ├── Create.cshtml
│       └── Edit.cshtml
│
├── wwwroot
│   ├── css
│   │   └── site.css
│   │
│   └── js
│       └── site.js
│
├── appsettings.json
├── Program.cs
└── SistemaMVCOracle.csproj
:. . / .
